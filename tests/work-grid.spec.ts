import { test } from "@playwright/test";

test.describe("Featured Work Grid", () => {
  test.skip("WORK-01: grid displays 6 project cards with thumbnail, title, category", async () => {});

  test.skip("WORK-02: hovering card triggers video element play", async () => {});
});
