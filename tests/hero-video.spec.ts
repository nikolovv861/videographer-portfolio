import { test } from "@playwright/test";

test.describe("Hero Section", () => {
  test.skip("HERO-01: hero section has video/iframe with autoplay attributes", async () => {});

  test.skip("HERO-02: headline and subheadline visible over video background", async () => {});

  test.skip("HERO-03: two CTA buttons present -- View My Work and Start a Project", async () => {});

  test.skip("HERO-04: hero content changes opacity/position on scroll (parallax)", async () => {});
});
