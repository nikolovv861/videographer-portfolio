---
phase: 03-content-sections
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/sections/Clients.tsx
  - components/sections/About.tsx
  - components/sections/Testimonials.tsx
  - components/sections/Contact.tsx
autonomous: true
requirements:
  - CLNT-01
  - CLNT-02
  - ABUT-01
  - ABUT-02
  - TEST-01
  - TEST-02
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "Client logos section shows auto-scrolling infinite marquee with 8-10 placeholder logo boxes"
    - "About section displays a photo placeholder, bio text, and philosophy pull quote on desktop side-by-side"
    - "Testimonials section shows 3 bordered quote cards with name and company"
    - "Contact section shows heading, message, email link, and two CTA buttons (no form)"
  artifacts:
    - path: "components/sections/Clients.tsx"
      provides: "Client logos marquee section"
      exports: ["Clients"]
      min_lines: 30
    - path: "components/sections/About.tsx"
      provides: "About section with photo, bio, philosophy"
      exports: ["About"]
      min_lines: 30
    - path: "components/sections/Testimonials.tsx"
      provides: "Testimonials section with quote cards"
      exports: ["Testimonials"]
      min_lines: 20
    - path: "components/sections/Contact.tsx"
      provides: "Simplified contact section with mailto CTAs"
      exports: ["Contact"]
      min_lines: 20
  key_links:
    - from: "components/sections/Clients.tsx"
      to: "data/clients.ts"
      via: "import { clients }"
      pattern: "import.*clients.*from.*data/clients"
    - from: "components/sections/About.tsx"
      to: "data/about.ts"
      via: "import { aboutContent }"
      pattern: "import.*aboutContent.*from.*data/about"
    - from: "components/sections/Testimonials.tsx"
      to: "data/site.ts"
      via: "import { testimonials }"
      pattern: "import.*testimonials.*from.*data/site"
    - from: "components/sections/Contact.tsx"
      to: "data/navigation.ts"
      via: "import { siteConfig }"
      pattern: "import.*siteConfig.*from.*data/navigation"
---

<objective>
Build the Clients marquee, About, Testimonials, and Contact section components.

Purpose: Delivers the remaining four content sections, including the marquee animation (most technically interesting piece) and the simplified contact section (no form per user decision).
Output: Four production section components ready for page wiring.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-sections/03-CONTEXT.md
@.planning/phases/03-content-sections/03-RESEARCH.md
@.planning/phases/03-content-sections/03-01-SUMMARY.md
@app/page.tsx
@app/globals.css
@data/navigation.ts
@components/sections/ClientsPlaceholder.tsx
@components/sections/AboutPlaceholder.tsx
@components/sections/TestimonialsPlaceholder.tsx
@components/sections/ContactPlaceholder.tsx
@components/ui/SectionWrapper.tsx
@components/ui/Button.tsx

<interfaces>
From lib/types.ts (created in Plan 01):
```typescript
export interface AboutContent {
  photoUrl: string;
  bio: string;
  philosophy: string;
}
export interface Client {
  id: string;
  name: string;
  logoUrl: string;
}
export interface Testimonial {
  id: string;
  quote: string;
  name: string;
  company: string;
}
export interface SiteConfig {
  name: string;
  tagline: string;
  email: string;
  instagram: string;
  linkedin: string;
  copyright: string;
  ctaText: string;
  ctaHref: string;
}
```

From data/about.ts (created in Plan 01):
```typescript
export const aboutContent: AboutContent;
```

From data/clients.ts (created in Plan 01):
```typescript
export const clients: Client[]; // 8 entries
```

From components/ui/SectionWrapper.tsx:
```typescript
// Props: id: string, fullBleed?: boolean, children: ReactNode
export function SectionWrapper({ id, fullBleed, children })
```

From components/ui/Button.tsx:
```typescript
export function Button({ variant, children, href, className, onClick }: ButtonProps)
// variant: "default" | "cta"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clients marquee and About section</name>
  <files>components/sections/Clients.tsx, components/sections/About.tsx</files>
  <action>
1. Create components/sections/Clients.tsx:
   - Import clients from @/data/clients, SectionWrapper
   - Use SectionWrapper with id="clients" and fullBleed prop
   - Inner container for heading: `<div className="mx-auto max-w-6xl px-6 md:px-8">` with centered h2 "Trusted By" using heading classes
   - IMPORTANT fullBleed pitfall: heading needs its own max-w-6xl container, but marquee must overflow full width
   - Marquee container: `<div className="mt-16 overflow-hidden">`
   - Inner marquee track: `<div className="flex" style={{ animation: "marquee 30s linear infinite" }}>` -- use inline style referencing the keyframes from globals.css (since Tailwind v4 does not auto-generate utility classes for custom keyframes without explicit @utility definition)
   - Alternatively, add `hover:[animation-play-state:paused]` for accessibility (pause on hover)
   - Render clients array TWICE for seamless loop: `{[...clients, ...clients].map((client, i) => ...)}`
   - Each logo box: `<div key={i} className="mx-6 flex h-16 w-32 flex-shrink-0 items-center justify-center rounded bg-foreground/5"><span className="text-xs text-body">{client.name}</span></div>`
   - The CSS animation translateX(-50%) makes the first set scroll away while the duplicate seamlessly replaces it

2. Create components/sections/About.tsx:
   - Import aboutContent from @/data/about, siteConfig from @/data/navigation, SectionWrapper
   - Import User icon from lucide-react (for photo placeholder silhouette)
   - SectionWrapper with id="about"
   - h2 heading "About" using established heading classes
   - mt-16 grid grid-cols-1 gap-12 lg:grid-cols-2 lg:gap-16
   - Left column: photo placeholder -- `<div className="aspect-[4/5] flex items-center justify-center rounded bg-foreground/5"><User className="h-16 w-16 text-foreground/20" /></div>`
   - Right column: `<div className="flex flex-col justify-center">`
     - Bio paragraph: `<p className="text-base text-body md:text-lg">{aboutContent.bio}</p>`
     - Philosophy as pull quote with gold left border (Claude's discretion): `<blockquote className="mt-8 border-l-2 border-gold pl-6 text-base text-heading/80 italic md:text-lg">{aboutContent.philosophy}</blockquote>`
   - Philosophy pull quote style creates visual hierarchy and uses gold accent language per research recommendation
  </action>
  <verify>
    <automated>cd /home/nikolovn/videographer-portfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Clients.tsx renders an auto-scrolling infinite marquee of 8 placeholder logos that pauses on hover. About.tsx renders a side-by-side layout with photo placeholder (silhouette icon), bio text, and philosophy pull quote with gold border accent.</done>
</task>

<task type="auto">
  <name>Task 2: Testimonials and Contact sections</name>
  <files>components/sections/Testimonials.tsx, components/sections/Contact.tsx</files>
  <action>
1. Create components/sections/Testimonials.tsx:
   - Import testimonials from @/data/site, SectionWrapper
   - SectionWrapper with id="testimonials"
   - h2 heading "Testimonials" using established heading classes
   - mt-16 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8
   - Each card: `<div key={testimonial.id} className="rounded-sm border border-foreground/10 p-8 transition-colors duration-300 hover:border-gold/40">`
   - blockquote: `<blockquote className="text-base text-body md:text-lg">&ldquo;{testimonial.quote}&rdquo;</blockquote>`
   - Attribution: mt-6 div with name (font-heading text-sm font-semibold text-heading) and company (mt-1 text-sm text-body)
   - Hover effect: gold border transition matching service/process cards

2. Create components/sections/Contact.tsx:
   - IMPORTANT: No form, no form validation. User explicitly simplified this section.
   - Import siteConfig from @/data/navigation, SectionWrapper, Button
   - SectionWrapper with id="contact"
   - Centered layout: `<div className="mx-auto max-w-2xl text-center">`
   - h2 heading "Get in Touch" using established heading classes
   - Descriptive paragraph: `<p className="mt-6 text-base text-body md:text-lg">Have a project in mind? Let&apos;s create something extraordinary together.</p>`
   - Email link: `<a href={mailto:${siteConfig.email}} className="mt-4 inline-block text-base text-heading transition-colors duration-300 hover:text-gold md:text-lg">{siteConfig.email}</a>`
   - Two CTA buttons in `<div className="mt-8 flex flex-col items-center gap-4 sm:flex-row sm:justify-center">`:
     - `<Button variant="cta" href={mailto:${siteConfig.email}}>Start a Project</Button>`
     - `<Button href="#">Book a Call</Button>` (placeholder href)
   - CONT-01 is satisfied by heading + message + email link (modified per user decision, no form)
   - CONT-02 is N/A (no form, no validation needed)
   - CONT-03 is satisfied by the direct email link
   - CONT-04 is satisfied by the two CTA buttons
  </action>
  <verify>
    <automated>cd /home/nikolovn/videographer-portfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Testimonials.tsx renders 3 bordered quote cards with hover effect, quote text, name, and company from data/site.ts. Contact.tsx renders a centered section with heading, message, email link, "Start a Project" mailto button, and "Book a Call" placeholder button. No form or validation code exists.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no type errors
- Clients.tsx uses fullBleed SectionWrapper and CSS marquee animation
- About.tsx uses data from data/about.ts with philosophy as pull quote
- Testimonials.tsx imports from data/site.ts
- Contact.tsx has NO form, NO form validation code
- Contact.tsx has exactly two CTA buttons and a direct email link
- All text from data imports, zero hardcoded content strings
</verification>

<success_criteria>
- Clients section renders infinite auto-scrolling marquee with 8 placeholder logos
- About section renders photo placeholder with User icon, bio, and gold-bordered philosophy quote
- About section responsive: stacked on mobile, side-by-side on desktop
- Testimonials section renders 3 quote cards with hover effects
- Contact section shows heading, message, email link, and two CTA buttons (no form)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-02-SUMMARY.md`
</output>
