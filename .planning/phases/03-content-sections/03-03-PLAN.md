---
phase: 03-content-sections
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - components/sections/Footer.tsx
  - app/page.tsx
  - tests/services.spec.ts
  - tests/clients.spec.ts
  - tests/about.spec.ts
  - tests/process.spec.ts
  - tests/testimonials.spec.ts
  - tests/contact.spec.ts
  - tests/footer.spec.ts
autonomous: false
requirements:
  - FOOT-01
  - FOOT-02

must_haves:
  truths:
    - "Footer displays Lucide Instagram and LinkedIn icons instead of text abbreviations"
    - "All placeholder components are replaced with real section components in page.tsx"
    - "All placeholder component files are deleted from the codebase"
    - "The full page renders all sections in correct order without errors"
  artifacts:
    - path: "components/sections/Footer.tsx"
      provides: "Footer with Lucide social icons"
      contains: "Instagram"
    - path: "app/page.tsx"
      provides: "Page with all real section imports"
      contains: "import { Services }"
    - path: "tests/services.spec.ts"
      provides: "E2E test stubs for services section"
    - path: "tests/contact.spec.ts"
      provides: "E2E test stubs for contact section"
    - path: "tests/footer.spec.ts"
      provides: "E2E test stubs for footer"
  key_links:
    - from: "components/sections/Footer.tsx"
      to: "lucide-react"
      via: "import { Instagram, Linkedin }"
      pattern: "import.*Instagram.*Linkedin.*from.*lucide-react"
    - from: "app/page.tsx"
      to: "components/sections/Services.tsx"
      via: "import { Services }"
      pattern: "import.*Services.*from.*components/sections/Services"
    - from: "app/page.tsx"
      to: "components/sections/Contact.tsx"
      via: "import { Contact }"
      pattern: "import.*Contact.*from.*components/sections/Contact"
---

<objective>
Upgrade Footer with Lucide icons, wire all new sections into page.tsx, delete placeholder files, create test stubs, and verify the complete page.

Purpose: Final integration step that replaces all placeholders with production components and verifies the full single-page layout renders correctly.
Output: Complete page with all content sections, cleaned-up codebase with no placeholder files, and e2e test stubs for phase verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-sections/03-CONTEXT.md
@.planning/phases/03-content-sections/03-01-SUMMARY.md
@.planning/phases/03-content-sections/03-02-SUMMARY.md
@app/page.tsx
@components/sections/Footer.tsx

<interfaces>
From components/sections/ (created in Plans 01-02):
```typescript
export function Services(): JSX.Element;    // id="services"
export function Process(): JSX.Element;     // id="process"
export function Clients(): JSX.Element;     // id="clients"
export function About(): JSX.Element;       // id="about"
export function Testimonials(): JSX.Element; // id="testimonials"
export function Contact(): JSX.Element;     // id="contact"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Footer upgrade, page wiring, placeholder cleanup, and test stubs</name>
  <files>components/sections/Footer.tsx, app/page.tsx, tests/services.spec.ts, tests/clients.spec.ts, tests/about.spec.ts, tests/process.spec.ts, tests/testimonials.spec.ts, tests/contact.spec.ts, tests/footer.spec.ts</files>
  <action>
1. Update components/sections/Footer.tsx:
   - Add import: `import { Instagram, Linkedin } from "lucide-react";`
   - Replace the "IG" text content with `<Instagram className="h-5 w-5" />`
   - Replace the "LI" text content with `<Linkedin className="h-5 w-5" />`
   - Keep ALL existing structure, classes, and layout unchanged
   - Keep existing aria-labels ("Instagram", "LinkedIn")

2. Update app/page.tsx:
   - Replace imports: ServicesPlaceholder -> Services, ClientsPlaceholder -> Clients, AboutPlaceholder -> About, ProcessPlaceholder -> Process, TestimonialsPlaceholder -> Testimonials, ContactPlaceholder -> Contact
   - Update component usage in JSX to use new component names (Services, Clients, About, Process, Testimonials, Contact)
   - Keep Hero, FeaturedWork, Showreel, Footer, SectionObserver imports unchanged
   - Keep all SectionObserver wrappers and sectionId props unchanged

3. Delete placeholder files (6 files):
   - components/sections/ServicesPlaceholder.tsx
   - components/sections/ClientsPlaceholder.tsx
   - components/sections/AboutPlaceholder.tsx
   - components/sections/ProcessPlaceholder.tsx
   - components/sections/TestimonialsPlaceholder.tsx
   - components/sections/ContactPlaceholder.tsx

4. Create e2e test stub files (all using test.skip pattern matching existing tests):

   tests/services.spec.ts:
   ```typescript
   import { test } from "@playwright/test";
   test.describe("Services Section", () => {
     test.skip("SERV-01: service cards render with Lucide icons, titles, and descriptions", async () => {});
     test.skip("SERV-01: section-level CTA button links to #contact", async () => {});
     test.skip("SERV-02: four specific services displayed (Commercial, Brand Story, Social Media, Event)", async () => {});
   });
   ```

   tests/clients.spec.ts:
   ```typescript
   import { test } from "@playwright/test";
   test.describe("Clients Section", () => {
     test.skip("CLNT-01: client logo marquee renders placeholder logos", async () => {});
     test.skip("CLNT-02: section heading Trusted By is visible", async () => {});
   });
   ```

   tests/about.spec.ts:
   ```typescript
   import { test } from "@playwright/test";
   test.describe("About Section", () => {
     test.skip("ABUT-01: about section displays photo placeholder, bio text, and philosophy", async () => {});
     test.skip("ABUT-02: about layout is side-by-side on desktop and stacked on mobile", async () => {});
   });
   ```

   tests/process.spec.ts:
   ```typescript
   import { test } from "@playwright/test";
   test.describe("Process Section", () => {
     test.skip("PROC-01: four process steps displayed with visual step numbers", async () => {});
     test.skip("PROC-02: each step has title and description", async () => {});
   });
   ```

   tests/testimonials.spec.ts:
   ```typescript
   import { test } from "@playwright/test";
   test.describe("Testimonials Section", () => {
     test.skip("TEST-01: testimonial cards display quote, name, and company", async () => {});
     test.skip("TEST-02: testimonial content comes from swappable data file", async () => {});
   });
   ```

   tests/contact.spec.ts:
   ```typescript
   import { test } from "@playwright/test";
   test.describe("Contact Section", () => {
     test.skip("CONT-01: contact section displays heading and message (no form)", async () => {});
     test.skip("CONT-03: direct email link is displayed and clickable", async () => {});
     test.skip("CONT-04: two CTA buttons present -- Start a Project and Book a Call", async () => {});
   });
   ```

   tests/footer.spec.ts:
   ```typescript
   import { test } from "@playwright/test";
   test.describe("Footer", () => {
     test.skip("FOOT-01: footer displays email, Instagram icon, and LinkedIn icon", async () => {});
     test.skip("FOOT-02: footer has copyright text and logo name", async () => {});
   });
   ```
  </action>
  <verify>
    <automated>cd /home/nikolovn/videographer-portfolio && npx tsc --noEmit 2>&1 | head -20 && npm run test 2>&1 | tail -10</automated>
  </verify>
  <done>Footer uses Lucide Instagram/Linkedin icons. page.tsx imports all real section components. All 6 placeholder files deleted. 7 e2e test stub files created. TypeScript compiles and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 3 content sections</name>
  <files></files>
  <action>
  Present the complete page to the user for visual verification of all Phase 3 sections.
  </action>
  <verify>User confirms all sections render correctly on desktop and mobile.</verify>
  <done>User approves the visual appearance of all content sections.</done>
  <what-built>All Phase 3 content sections: Services (4 cards with gold Lucide icons + section CTA), Clients (infinite marquee with 8 logos), About (photo placeholder + bio + philosophy pull quote), Process (4 step cards with large numbers), Testimonials (3 quote cards), Contact (heading + email + 2 CTA buttons, no form), and Footer (Lucide social icons). All placeholders replaced and deleted.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000
    2. Scroll through the entire page and verify each section:
       - Services: 4 cards with gold icons, titles, descriptions, and a "Start a Project" CTA at bottom
       - Clients: Logos auto-scroll horizontally in an infinite loop, pause on hover
       - About: Photo placeholder (gray box with silhouette) on left, bio on right, philosophy in gold-bordered pull quote
       - Process: 4 cards with large faded "01"-"04" numbers, titles, and descriptions
       - Testimonials: 3 bordered cards with quotes, names, companies
       - Contact: Centered heading, message, email link, "Start a Project" and "Book a Call" buttons
       - Footer: Instagram and LinkedIn shown as icons (not "IG"/"LI" text)
    3. Resize browser to mobile width and verify:
       - About section stacks vertically (photo on top, text below)
       - All grids collapse to single column
       - Marquee still scrolls
    4. Verify no visual regressions on Hero, Featured Work, and Showreel sections
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run test` passes (all test stubs are skipped, existing tests still pass)
- No *Placeholder.tsx files remain in components/sections/
- page.tsx has zero placeholder imports
- Footer renders Lucide icons, not text
- Full page loads without console errors
</verification>

<success_criteria>
- Footer displays Lucide Instagram and LinkedIn icons
- page.tsx imports all 6 real section components (no placeholder references)
- All 6 placeholder component files deleted
- 7 e2e test stub files created covering all Phase 3 requirement IDs
- TypeScript compiles, existing tests pass
- User visually confirms all sections render correctly on desktop and mobile
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-03-SUMMARY.md`
</output>
