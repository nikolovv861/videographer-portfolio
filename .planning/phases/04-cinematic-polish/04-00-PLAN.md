---
phase: 04-cinematic-polish
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/loading-intro.spec.ts
  - tests/custom-cursor.spec.ts
  - tests/film-grain.spec.ts
  - tests/scroll-animations.spec.ts
  - tests/gold-dividers.spec.ts
  - tests/reduced-motion.spec.ts
  - tests/lazy-loading.spec.ts
  - tests/seo-meta.spec.ts
autonomous: true
requirements: [FX-01, FX-02, FX-03, FX-04, FX-05, FX-06, FX-07, PERF-01, PERF-02]

must_haves:
  truths:
    - "All 8 Playwright test spec files exist with meaningful test stubs"
    - "Each spec file has at least one test that will fail until the feature is implemented (RED state)"
    - "Running the full test suite completes without syntax errors (tests fail on assertions, not on parse errors)"
  artifacts:
    - path: "tests/loading-intro.spec.ts"
      provides: "Test stubs for cinema letterbox loading intro and return visit behavior"
      contains: "sessionStorage"
    - path: "tests/custom-cursor.spec.ts"
      provides: "Test stubs for custom cursor dot + ring on desktop"
      contains: "cursor"
    - path: "tests/film-grain.spec.ts"
      provides: "Test stubs for film grain SVG filter overlay"
      contains: "feTurbulence"
    - path: "tests/scroll-animations.spec.ts"
      provides: "Test stubs for scroll-triggered section reveals"
      contains: "scroll"
    - path: "tests/gold-dividers.spec.ts"
      provides: "Test stubs for gold divider center-outward animation"
      contains: "divider"
    - path: "tests/reduced-motion.spec.ts"
      provides: "Test stubs for prefers-reduced-motion compliance"
      contains: "prefers-reduced-motion"
    - path: "tests/lazy-loading.spec.ts"
      provides: "Test stubs for lazy-loaded Vimeo iframes"
      contains: "iframe"
    - path: "tests/seo-meta.spec.ts"
      provides: "Test stubs for meta tags, OG, Twitter, JSON-LD"
      contains: "og:"
  key_links: []
---

<objective>
Create Playwright test stubs for all Phase 4 cinematic polish features. These tests define the expected behaviors before implementation begins, establishing the RED state for the Nyquist validation strategy.

Purpose: Wave 0 test scaffolding ensures every task in Wave 1-3 has an automated verification command ready. Tests will fail until their corresponding features are implemented.
Output: 8 Playwright spec files in tests/ directory.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-cinematic-polish/04-CONTEXT.md
@.planning/phases/04-cinematic-polish/04-VALIDATION.md

<interfaces>
<!-- Existing test pattern from Phase 1-3 specs -->

From tests/hero-video.spec.ts (example pattern):
```typescript
import { test, expect } from "@playwright/test";

test.describe("Hero Section", () => {
  test.beforeEach(async ({ page }) => {
    await page.goto("/");
  });

  test("renders hero section", async ({ page }) => {
    const hero = page.locator('[id="hero"]');
    await expect(hero).toBeVisible();
  });
});
```

From playwright.config.ts:
```typescript
// Chromium project configured, baseURL likely http://localhost:3000
// Tests run against dev server
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for cinematic effects (loading-intro, custom-cursor, film-grain, scroll-animations, gold-dividers, reduced-motion)</name>
  <files>tests/loading-intro.spec.ts, tests/custom-cursor.spec.ts, tests/film-grain.spec.ts, tests/scroll-animations.spec.ts, tests/gold-dividers.spec.ts, tests/reduced-motion.spec.ts</files>
  <action>
Create 6 Playwright spec files following the existing test patterns in the project. Each spec should have a `test.beforeEach` that navigates to `/` and contain test stubs with `.skip` or expected-to-fail assertions. The tests define WHAT should be true once features are built.

1. `tests/loading-intro.spec.ts` (FX-01, FX-02):
   - Test: "shows letterbox loading overlay on first visit" -- expect a fixed overlay element at z-200 to be visible, with a percentage counter element present
   - Test: "shows percentage counter counting to 100%" -- expect text content matching /\d+%/ pattern in the overlay
   - Test: "hides overlay after intro completes" -- wait ~4 seconds, expect overlay to not be visible (AnimatePresence removes it)
   - Test: "shows quick animation on return visit" -- navigate to /, wait for intro to finish, navigate away and back, expect no percentage counter visible (sessionStorage skip)
   - Test: "skips intro for reduced-motion users" -- emulate prefers-reduced-motion: reduce, expect overlay to not be visible or immediately hidden

2. `tests/custom-cursor.spec.ts` (FX-03):
   - Test: "renders custom cursor dot and ring on desktop" -- expect two pointer-events-none fixed elements with z-[90] to exist
   - Test: "cursor follows mouse movement" -- move mouse to coordinates, check that cursor elements' transform/position updates
   - Test: "ring expands on interactive element hover" -- hover over a link/button, check ring element has expanded scale or gold background
   - Test: "hides custom cursor on touch devices" -- emulate touch device (use `hasTouch: true` in context), expect cursor elements to not exist

3. `tests/film-grain.spec.ts` (FX-04):
   - Test: "renders film grain SVG filter in hero section" -- expect an SVG element with feTurbulence filter within or near the hero section
   - Test: "film grain has low opacity" -- check the grain container element has opacity style between 0.04 and 0.10

4. `tests/scroll-animations.spec.ts` (FX-05):
   - Test: "sections have scroll-triggered animation wrappers" -- scroll to a section (e.g., services), expect elements with motion-related attributes (style containing opacity or transform) to exist
   - Test: "section content becomes visible after scrolling into view" -- scroll to a below-fold section, wait briefly, expect content to have opacity: 1

5. `tests/gold-dividers.spec.ts` (FX-06):
   - Test: "gold divider elements exist between sections" -- expect multiple elements matching gold divider pattern (bg-gold or #c9a96e colored, h-px height) to be in the DOM
   - Test: "dividers animate on scroll" -- scroll to a divider, expect it to have scaleX(1) or visible width after being in viewport

6. `tests/reduced-motion.spec.ts` (FX-07):
   - Test: "CSS disables animations for reduced-motion users" -- emulate prefers-reduced-motion: reduce, check that computed animation-duration on an animated element is 0.01ms or similar
   - Test: "custom cursor hidden for reduced-motion users" -- emulate reduced motion, expect cursor elements to not exist
   - Test: "loading intro skipped for reduced-motion users" -- emulate reduced motion, expect no visible overlay on page load

For each spec file, use `test.describe` grouping and follow the existing pattern of `test.beforeEach` with `page.goto("/")`. Where tests cannot pass yet, use clear assertion expectations that will naturally fail (RED state) -- do NOT use `test.skip()` as we want to see them fail.
  </action>
  <verify>
    <automated>cd /home/nikolovn/videographer-portfolio && ls tests/loading-intro.spec.ts tests/custom-cursor.spec.ts tests/film-grain.spec.ts tests/scroll-animations.spec.ts tests/gold-dividers.spec.ts tests/reduced-motion.spec.ts && echo "All 6 effect spec files exist"</automated>
  </verify>
  <done>6 Playwright spec files created with meaningful test stubs covering FX-01 through FX-07</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for performance and SEO (lazy-loading, seo-meta)</name>
  <files>tests/lazy-loading.spec.ts, tests/seo-meta.spec.ts</files>
  <action>
Create 2 Playwright spec files for performance and SEO requirements.

1. `tests/lazy-loading.spec.ts` (PERF-01):
   - Test: "hero video iframe loads eagerly (above fold)" -- expect an iframe within the hero section to be present in DOM on initial load
   - Test: "below-fold video iframes are lazy-loaded" -- check that Vimeo iframes in below-fold sections (e.g., showreel) are NOT present in DOM initially, then scroll near them and expect them to appear (intersection observer triggers load)
   - Use `page.evaluate` to check iframe presence without scrolling first, then scroll and recheck

2. `tests/seo-meta.spec.ts` (PERF-02):
   - Test: "page has title meta tag" -- expect `page.title()` to contain "Alex Rivera"
   - Test: "page has description meta tag" -- expect `meta[name="description"]` to have content
   - Test: "page has Open Graph tags" -- expect `meta[property="og:title"]`, `meta[property="og:description"]`, `meta[property="og:image"]` to exist with content
   - Test: "page has Twitter card tags" -- expect `meta[name="twitter:card"]`, `meta[name="twitter:title"]` to exist
   - Test: "page has JSON-LD structured data" -- expect `script[type="application/ld+json"]` to exist, parse its content, check it has @type and name fields

Follow same patterns as Task 1.
  </action>
  <verify>
    <automated>cd /home/nikolovn/videographer-portfolio && ls tests/lazy-loading.spec.ts tests/seo-meta.spec.ts && echo "Both perf/SEO spec files exist"</automated>
  </verify>
  <done>2 Playwright spec files created with test stubs covering PERF-01 and PERF-02</done>
</task>

</tasks>

<verification>
- All 8 spec files exist in tests/ directory
- Each file follows existing Playwright patterns (import, describe, beforeEach)
- Tests have meaningful assertions that will fail until features are implemented
- No syntax errors in spec files: `npx playwright test --list 2>&1 | grep -c "spec.ts"` shows all specs listed
</verification>

<success_criteria>
- 8 new Playwright spec files created: loading-intro, custom-cursor, film-grain, scroll-animations, gold-dividers, reduced-motion, lazy-loading, seo-meta
- Each spec has at least 2 test cases with real assertions
- Files parse without syntax errors
- Wave 0 Requirements checklist in VALIDATION.md can be marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-cinematic-polish/04-00-SUMMARY.md`
</output>
