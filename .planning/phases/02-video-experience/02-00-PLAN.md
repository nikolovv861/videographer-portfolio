---
phase: 02-video-experience
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - data/projects.ts
  - data/hero.ts
  - tests/hero-video.spec.ts
  - tests/work-grid.spec.ts
  - tests/project-modal.spec.ts
  - tests/showreel.spec.ts
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
  - WORK-05
  - WORK-06
  - REEL-01
  - REEL-02
  - REEL-03

must_haves:
  truths:
    - "Project type includes vimeoId field for Vimeo embeds"
    - "Hero content data (headline, tagline, Vimeo ID) exists in a data file"
    - "All 13 Phase 2 requirements have corresponding test stubs"
  artifacts:
    - path: "lib/types.ts"
      provides: "Extended Project type with vimeoId"
      contains: "vimeoId"
    - path: "data/hero.ts"
      provides: "Hero section content data"
      exports: ["heroContent"]
    - path: "tests/hero-video.spec.ts"
      provides: "E2E test stubs for HERO-01 through HERO-04"
    - path: "tests/work-grid.spec.ts"
      provides: "E2E test stubs for WORK-01, WORK-02"
    - path: "tests/project-modal.spec.ts"
      provides: "E2E test stubs for WORK-03 through WORK-06"
    - path: "tests/showreel.spec.ts"
      provides: "E2E test stubs for REEL-01 through REEL-03"
  key_links:
    - from: "data/projects.ts"
      to: "lib/types.ts"
      via: "import type { Project }"
      pattern: "vimeoId"
---

<objective>
Extend data contracts and create test infrastructure for Phase 2.

Purpose: Establish the type extensions (vimeoId on Project), hero content data file, and Playwright test stubs that all subsequent Phase 2 plans build against.
Output: Updated types, hero data, and 4 test files with stubs for all 13 requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-video-experience/02-CONTEXT.md
@.planning/phases/02-video-experience/02-RESEARCH.md
@.planning/phases/02-video-experience/02-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/types.ts:
```typescript
export interface Project {
  id: string;
  title: string;
  category: string;
  client: string;
  year: string;
  role: string;
  type: string;
  thumbnailUrl: string;
  videoUrl: string;
  challenge: string;
  approach: string;
  result: string;
  gallery: string[];
}

export interface SiteConfig {
  name: string;
  tagline: string;
  email: string;
  instagram: string;
  linkedin: string;
  copyright: string;
  ctaText: string;
  ctaHref: string;
}
```

From data/navigation.ts:
```typescript
export const siteConfig: SiteConfig = {
  name: "Alex Rivera",
  tagline: "Cinematic Storytelling",
  // ...
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Project type and data with vimeoId, create hero data file</name>
  <files>lib/types.ts, data/projects.ts, data/hero.ts</files>
  <action>
1. In `lib/types.ts`, add `vimeoId?: string` to the `Project` interface (optional field, after `videoUrl`).

2. In `data/projects.ts`, add a placeholder `vimeoId` to each of the 6 projects. Use realistic-looking placeholder IDs like `"123456789"`, `"234567890"`, etc. These are just placeholder Vimeo video IDs -- the actual IDs will be swapped in when real videos are available.

3. Create `data/hero.ts` with a `heroContent` export containing:
   - `headline`: `"Alex Rivera"` (from siteConfig.name)
   - `tagline`: `"Cinematic Storytelling"` (from siteConfig.tagline)
   - `vimeoId`: `"987654321"` (placeholder hero showreel Vimeo ID)
   - `ctaWork`: `{ label: "View My Work", href: "#work" }`
   - `ctaContact`: `{ label: "Start a Project", href: "#contact" }`

Export the type `HeroContent` as well. Do NOT import from siteConfig -- keep hero data self-contained so it can be customized independently of the nav config.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Project type has vimeoId field, all 6 projects have placeholder vimeoIds, hero data file exists with typed export</done>
</task>

<task type="auto">
  <name>Task 2: Create Playwright test stubs for all Phase 2 requirements</name>
  <files>tests/hero-video.spec.ts, tests/work-grid.spec.ts, tests/project-modal.spec.ts, tests/showreel.spec.ts</files>
  <action>
Create 4 Playwright test files with `test.skip()` stubs (NOT `test.todo()` -- Playwright does not support it, per Phase 1 decision). Each stub should have a descriptive test name mapping to its requirement ID.

**tests/hero-video.spec.ts** (HERO-01 through HERO-04):
```
test.skip('HERO-01: hero section has video/iframe with autoplay attributes')
test.skip('HERO-02: headline and subheadline visible over video background')
test.skip('HERO-03: two CTA buttons present -- View My Work and Start a Project')
test.skip('HERO-04: hero content changes opacity/position on scroll (parallax)')
```

**tests/work-grid.spec.ts** (WORK-01, WORK-02):
```
test.skip('WORK-01: grid displays 6 project cards with thumbnail, title, category')
test.skip('WORK-02: hovering card triggers video element play')
```

**tests/project-modal.spec.ts** (WORK-03 through WORK-06):
```
test.skip('WORK-03: clicking card opens full-screen modal with backdrop blur')
test.skip('WORK-04: modal contains video, metadata, narrative sections, gallery')
test.skip('WORK-05: modal closes via close button')
test.skip('WORK-05b: modal closes via backdrop click')
test.skip('WORK-05c: modal closes via Escape key')
test.skip('WORK-06: body scroll locked when modal open, position preserved on close')
```

**tests/showreel.spec.ts** (REEL-01 through REEL-03):
```
test.skip('REEL-01: showreel section has Vimeo iframe embed')
test.skip('REEL-02: description text displayed near player')
test.skip('REEL-03: two navigation buttons present -- Watch Showreel and See All Work')
```

Use `import { test } from '@playwright/test';` at the top of each file. Wrap related tests in `test.describe()` blocks (e.g., `test.describe('Hero Section', ...)`, `test.describe('Featured Work Grid', ...)`, etc.).
  </action>
  <verify>
    <automated>npx playwright test tests/hero-video.spec.ts tests/work-grid.spec.ts tests/project-modal.spec.ts tests/showreel.spec.ts --project=chromium --list 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 test files exist, Playwright recognizes all test stubs, tests are skipped (not failing)</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx playwright test tests/hero-video.spec.ts tests/work-grid.spec.ts tests/project-modal.spec.ts tests/showreel.spec.ts --project=chromium --list` shows all 13+ test stubs
</verification>

<success_criteria>
- Project type extended with vimeoId field
- All 6 projects have placeholder vimeoIds
- Hero content data file exists with typed exports
- 4 Playwright test files with stubs covering all 13 Phase 2 requirements
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-video-experience/02-00-SUMMARY.md`
</output>
