---
phase: 02-video-experience
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - components/sections/Showreel.tsx
  - app/page.tsx
autonomous: false
requirements:
  - REEL-01
  - REEL-02
  - REEL-03

must_haves:
  truths:
    - "Showreel section displays a Vimeo video player with controls"
    - "Description text is visible near the video player"
    - "Two buttons navigate to relevant sections"
    - "Page renders Hero, FeaturedWork, and Showreel instead of placeholders"
  artifacts:
    - path: "components/sections/Showreel.tsx"
      provides: "Showreel section with Vimeo embed and navigation buttons"
      min_lines: 25
    - path: "app/page.tsx"
      provides: "Updated page wiring with real sections replacing placeholders"
  key_links:
    - from: "components/sections/Showreel.tsx"
      to: "components/ui/VimeoEmbed.tsx"
      via: "import { VimeoEmbed }"
      pattern: "VimeoEmbed.*player"
    - from: "app/page.tsx"
      to: "components/sections/Hero.tsx"
      via: "import { Hero }"
      pattern: "Hero"
    - from: "app/page.tsx"
      to: "components/sections/FeaturedWork.tsx"
      via: "import { FeaturedWork }"
      pattern: "FeaturedWork"
    - from: "app/page.tsx"
      to: "components/sections/Showreel.tsx"
      via: "import { Showreel }"
      pattern: "Showreel"
---

<objective>
Build the showreel section and wire all Phase 2 sections into the page, then verify the full video experience.

Purpose: Complete the phase by adding the showreel video player section and replacing all three placeholder components with the real implementations. Human verification confirms the full experience works.
Output: Showreel.tsx, updated page.tsx, visual confirmation of the complete video experience.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-video-experience/02-CONTEXT.md
@.planning/phases/02-video-experience/02-RESEARCH.md
@.planning/phases/02-video-experience/02-01-SUMMARY.md
@.planning/phases/02-video-experience/02-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 01 and 02 -->

From components/ui/VimeoEmbed.tsx (Plan 01):
```typescript
export function VimeoEmbed({ vimeoId, mode, className? }: {
  vimeoId: string;
  mode: "background" | "player";
  className?: string;
}): JSX.Element;
```

From components/sections/Hero.tsx (Plan 01):
```typescript
export function Hero(): JSX.Element;  // Client component, renders #hero section
```

From components/sections/FeaturedWork.tsx (Plan 02):
```typescript
export function FeaturedWork(): JSX.Element;  // Client component, renders #work section
```

From components/ui/Button.tsx:
```typescript
export function Button({ variant?, children, href?, className?, onClick? }): JSX.Element;
```

From components/ui/SectionWrapper.tsx:
```typescript
export function SectionWrapper({ id, children, fullBleed?, className? }): JSX.Element;
```

From data/hero.ts (Plan 00):
```typescript
export const heroContent: HeroContent;  // Contains vimeoId for showreel
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Showreel section and wire page</name>
  <files>components/sections/Showreel.tsx, app/page.tsx</files>
  <action>
**components/sections/Showreel.tsx** -- Showreel video player section:

Import `SectionWrapper`, `Button`, `VimeoEmbed` (player mode), and `heroContent` from `data/hero.ts` (uses the same showreel vimeoId).

Structure:
```
<SectionWrapper id="showreel">
  <div className="text-center">
    <h2 className="font-heading text-3xl font-bold tracking-tight text-heading md:text-4xl">
      Showreel
    </h2>
    <p className="mx-auto mt-6 max-w-2xl text-base text-body md:text-lg">
      A curated selection of my best work across commercials, brand films, and social content.
    </p>
  </div>

  {/* Vimeo player embed */}
  <div className="mt-16">
    <VimeoEmbed vimeoId={heroContent.vimeoId} mode="player" />
  </div>

  {/* Navigation buttons */}
  <div className="mt-10 flex flex-col items-center gap-4 sm:flex-row sm:justify-center">
    <Button href="#showreel">Watch Showreel</Button>
    <Button variant="cta" href="#work">See All Work</Button>
  </div>
</SectionWrapper>
```

The "Watch Showreel" button scrolls to the showreel section itself (anchor to #showreel -- useful when linked from elsewhere). The "See All Work" button scrolls to #work (the featured work grid).

Per REEL-02: The description text ("A curated selection...") is displayed below the heading and above/near the player.

Per REEL-03: Two buttons with clear labels. Uses existing Button component with default and cta variants.

**app/page.tsx** -- Replace placeholders with real components:

Update imports:
- Replace `HeroPlaceholder` import with `Hero` from `@/components/sections/Hero`
- Replace `WorkPlaceholder` import with `FeaturedWork` from `@/components/sections/FeaturedWork`
- Replace `ShowreelPlaceholder` import with `Showreel` from `@/components/sections/Showreel`

Update JSX:
- `<HeroPlaceholder />` becomes `<Hero />`
- `<WorkPlaceholder />` becomes `<FeaturedWork />`
- `<ShowreelPlaceholder />` becomes `<Showreel />`

Keep all other sections (Services, Clients, About, Process, Testimonials, Contact, Footer) unchanged -- they remain as placeholders for Phase 3.

Keep the `<SectionObserver>` wrappers around each section -- they handle the active nav highlighting.

Do NOT delete the placeholder files (HeroPlaceholder.tsx, WorkPlaceholder.tsx, ShowreelPlaceholder.tsx) -- they may serve as reference. They are simply no longer imported.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Showreel section displays Vimeo player embed with description and two nav buttons, page.tsx renders Hero/FeaturedWork/Showreel instead of placeholders, build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 2 video experience</name>
  <files>app/page.tsx</files>
  <action>
Human verification of the complete Phase 2 video experience. Run `npm run dev` and verify all sections visually.

What was built:
- Full-screen hero with Vimeo background video (or dev placeholder), headline overlay, two CTA buttons, parallax scroll effect, animated scroll-down chevron
- Featured Work grid with 6 project cards, hover video previews, gold accent interactions, click-to-open case study modals with scroll lock
- Showreel section with embedded Vimeo player, description text, two navigation buttons

Verification steps:
1. Run `npm run dev` and open http://localhost:3000
2. Hero section: Verify full-viewport hero with Vimeo iframe (or placeholder), headline "Alex Rivera", tagline "Cinematic Storytelling", two CTA buttons. Scroll down to see content fade and lift (parallax). Note the scroll-down chevron bouncing at the bottom.
3. Featured Work grid: Scroll to the Work section. Verify 6 project cards in a 3-column grid (desktop). Hover over a card to see video preview play (or placeholder behavior if no mp4 files in /public/videos/). Note gold border glow and subtle scale on hover.
4. Project Modal: Click any project card. Verify full-screen modal opens with backdrop blur. Check: video/placeholder at top, metadata bar (Client, Year, Role, Type), Challenge/Approach/Result narrative sections, gallery grid. Close via X button, then reopen and close via clicking outside the modal, then reopen and close via Escape key. Verify scroll position is preserved each time.
5. Showreel section: Scroll to the Showreel section. Verify Vimeo player iframe (or placeholder), description text, "Watch Showreel" and "See All Work" buttons.
6. Mobile: Resize to mobile width. Verify hero is full-screen, work grid collapses to single column, modal is usable, showreel player is responsive.
7. Navigation: Verify nav active state highlights correctly as you scroll through Hero, Work, and Showreel sections.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User approves the complete Phase 2 video experience -- hero, work grid, modals, and showreel all working correctly</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- Page renders 3 real sections (Hero, FeaturedWork, Showreel) plus 6 placeholder sections
- All Phase 2 Playwright test stubs can be unskipped and implemented
- Full visual verification by user
</verification>

<success_criteria>
- Showreel section has Vimeo iframe player with controls
- Description text visible near player
- Two navigation buttons functional
- page.tsx imports real Hero, FeaturedWork, Showreel components
- Build succeeds
- User approves the full Phase 2 video experience
</success_criteria>

<output>
After completion, create `.planning/phases/02-video-experience/02-03-SUMMARY.md`
</output>
