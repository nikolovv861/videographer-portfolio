---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/fonts.ts
  - lib/types.ts
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - data/navigation.ts
  - data/projects.ts
  - data/services.ts
  - data/site.ts
  - components/ui/Button.tsx
  - components/ui/SectionWrapper.tsx
  - components/sections/HeroPlaceholder.tsx
  - components/sections/WorkPlaceholder.tsx
  - components/sections/ShowreelPlaceholder.tsx
  - components/sections/ServicesPlaceholder.tsx
  - components/sections/ClientsPlaceholder.tsx
  - components/sections/AboutPlaceholder.tsx
  - components/sections/ProcessPlaceholder.tsx
  - components/sections/TestimonialsPlaceholder.tsx
  - components/sections/ContactPlaceholder.tsx
  - components/sections/Footer.tsx
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "Site renders with dark charcoal background (#0a0a0a), off-white headings (#f5f5f5), warm gray body text (#a0a0a0)"
    - "Syne font renders on headings, DM Sans on body text"
    - "Champagne gold (#c9a96e) appears on CTA button hover states"
    - "Page displays all placeholder sections in correct order with generous spacing"
    - "Layout is responsive across mobile, tablet, and desktop breakpoints"
    - "All placeholder content comes from static data files, not hardcoded in components"
  artifacts:
    - path: "app/globals.css"
      provides: "Tailwind @theme inline tokens for colors, fonts"
      contains: "--color-gold: #c9a96e"
    - path: "lib/fonts.ts"
      provides: "Syne + DM Sans font configuration"
      exports: ["syne", "dmSans"]
    - path: "lib/types.ts"
      provides: "Shared TypeScript interfaces"
      exports: ["NavItem", "Project", "Service", "Testimonial"]
    - path: "data/navigation.ts"
      provides: "Nav links and site config"
      exports: ["navItems", "siteConfig"]
    - path: "components/ui/SectionWrapper.tsx"
      provides: "Consistent section spacing and max-width"
      exports: ["SectionWrapper"]
    - path: "components/ui/Button.tsx"
      provides: "Button with default and cta variants"
      exports: ["Button"]
    - path: "app/layout.tsx"
      provides: "Root layout with Syne + DM Sans fonts"
      min_lines: 15
    - path: "app/page.tsx"
      provides: "Single page composing all sections"
      min_lines: 20
  key_links:
    - from: "app/layout.tsx"
      to: "lib/fonts.ts"
      via: "font CSS variable classes on html"
      pattern: "syne\\.variable.*dmSans\\.variable"
    - from: "app/globals.css"
      to: "lib/fonts.ts"
      via: "@theme inline mapping font vars"
      pattern: "--font-heading.*var\\(--font-syne\\)"
    - from: "components/sections/*"
      to: "data/*.ts"
      via: "import placeholder content"
      pattern: "import.*from.*@/data/"
---

<objective>
Design system, data architecture, and placeholder page layout for the videographer portfolio.

Purpose: Establish the visual foundation (colors, typography, spacing, responsive tokens) and data layer that every future component builds on. Creates the single-page structure with all section placeholders so Phase 1 navigation and all future phases have real sections to target.

Output: Working Next.js page with dark theme, Syne + DM Sans fonts, gold accent tokens, typed data files, reusable UI components (Button, SectionWrapper), and 10 placeholder section components rendered in order.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@app/layout.tsx
@app/globals.css
@app/page.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create design tokens, fonts, types, and data files</name>
  <files>
    lib/fonts.ts,
    lib/types.ts,
    app/globals.css,
    data/navigation.ts,
    data/projects.ts,
    data/services.ts,
    data/site.ts,
    package.json
  </files>
  <action>
1. Install dependencies:
   ```bash
   npm install react-intersection-observer lucide-react
   ```

2. Create `lib/fonts.ts` -- Syne + DM Sans via next/font/google:
   - Syne: variable `--font-syne`, subsets ["latin"], display "swap". Do NOT specify weight array (variable font).
   - DM_Sans: variable `--font-dm-sans`, subsets ["latin"], display "swap". Import as `DM_Sans` (underscore). Do NOT specify weight array.
   - Export both font objects.

3. Create `lib/types.ts` -- shared interfaces:
   - `NavItem`: { label: string, href: string, sectionId: string }
   - `Project`: { id: string, title: string, category: string, client: string, year: string, role: string, type: string, thumbnailUrl: string, videoUrl: string, challenge: string, approach: string, result: string, gallery: string[] }
   - `Service`: { id: string, title: string, description: string, icon: string }
   - `Testimonial`: { id: string, quote: string, name: string, company: string }
   - `ProcessStep`: { id: string, number: number, title: string, description: string }
   - `SiteConfig`: { name: string, tagline: string, email: string, instagram: string, linkedin: string, copyright: string, ctaText: string, ctaHref: string }

4. Replace `app/globals.css` entirely with the Phase 1 design tokens:
   ```css
   @import "tailwindcss";

   @theme inline {
     --color-background: #0a0a0a;
     --color-foreground: #f5f5f5;
     --color-heading: #f5f5f5;
     --color-body: #a0a0a0;
     --color-gold: #c9a96e;
     --font-heading: var(--font-syne);
     --font-body: var(--font-dm-sans);
   }

   html {
     scroll-behavior: smooth;
     scroll-padding-top: 5rem;
   }

   body {
     background-color: #0a0a0a;
     color: #a0a0a0;
     font-family: var(--font-dm-sans), ui-sans-serif, system-ui, sans-serif;
   }

   h1, h2, h3, h4, h5, h6 {
     font-family: var(--font-syne), ui-sans-serif, system-ui, sans-serif;
     color: #f5f5f5;
   }
   ```
   Remove the :root, dark media query, and Geist font references from the old file.

5. Create data files with typed placeholder content:
   - `data/navigation.ts`: navItems array (Work, Showreel, Services, About, Contact with matching sectionIds and href="#id") and siteConfig object (name: "Alex Rivera", tagline: "Cinematic Storytelling", ctaText: "Start a Project", ctaHref: "#contact").
   - `data/projects.ts`: 6 placeholder Project objects with realistic videography titles, categories (Commercial, Brand Film, Social Content, Corporate), placeholder URLs.
   - `data/services.ts`: 4 Service objects matching SERV-02 (Commercial Video Production, Brand Story Films, Social Media Content, Event and Corporate Films) with descriptions and lucide icon names.
   - `data/site.ts`: SiteConfig with name, tagline, email "hello@alexrivera.com", instagram/linkedin URLs, copyright text, plus ProcessStep array (Discovery, Concept, Production, Post Production) and Testimonial array (3 placeholder entries).

All data files must import types from `@/lib/types` and export typed constants.
  </action>
  <verify>
    <automated>cd /home/nikolovn/videographer-portfolio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Dependencies installed. lib/fonts.ts exports Syne + DM Sans. lib/types.ts exports all shared interfaces. globals.css has @theme inline tokens with correct colors and font mappings. Four data files export typed placeholder content. TypeScript compilation passes with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create UI components, placeholder sections, layout, and page</name>
  <files>
    components/ui/Button.tsx,
    components/ui/SectionWrapper.tsx,
    components/sections/HeroPlaceholder.tsx,
    components/sections/WorkPlaceholder.tsx,
    components/sections/ShowreelPlaceholder.tsx,
    components/sections/ServicesPlaceholder.tsx,
    components/sections/ClientsPlaceholder.tsx,
    components/sections/AboutPlaceholder.tsx,
    components/sections/ProcessPlaceholder.tsx,
    components/sections/TestimonialsPlaceholder.tsx,
    components/sections/ContactPlaceholder.tsx,
    components/sections/Footer.tsx,
    app/layout.tsx,
    app/page.tsx
  </files>
  <action>
1. Create `components/ui/Button.tsx`:
   - Props: variant ("default" | "cta"), children, href (optional), className (optional), onClick (optional)
   - Base styles: inline-flex items-center justify-center font-heading text-sm tracking-wide transition-all duration-300
   - default variant: border border-foreground/20 text-foreground px-6 py-3 hover:border-foreground/40
   - cta variant: border border-foreground/30 text-foreground px-6 py-3 hover:bg-gold hover:text-background hover:border-gold
   - Renders `<a>` if href provided, `<button>` otherwise
   - Per user decision: CTA is white/subtle at rest, fills champagne gold on hover (progressive reveal)

2. Create `components/ui/SectionWrapper.tsx`:
   - Props: id (string), children, fullBleed (boolean, default false), className (optional)
   - Renders `<section id={id}>` with py-32 md:py-48 for generous editorial spacing (per user decision)
   - If not fullBleed: inner div with mx-auto max-w-6xl px-6 md:px-8
   - If fullBleed: children rendered directly (for Hero, Clients)

3. Create 10 placeholder section components. Each is a server component that imports data from `@/data/*` and uses SectionWrapper. Each displays:
   - `HeroPlaceholder.tsx`: fullBleed, id="hero", full viewport height (min-h-screen), centered headline from siteConfig.name + siteConfig.tagline, two Button components (View My Work href="#work", Start a Project href="#contact" cta variant). Dark overlay gradient for future video background.
   - `WorkPlaceholder.tsx`: id="work", heading "Featured Work", grid of project cards (grid-cols-1 md:grid-cols-2 lg:grid-cols-3) from projects data showing title and category.
   - `ShowreelPlaceholder.tsx`: id="showreel", heading "Showreel", placeholder video area (aspect-video bg-foreground/5 rounded), description text, two buttons.
   - `ServicesPlaceholder.tsx`: id="services", heading "Services", grid of service cards from services data (grid-cols-1 md:grid-cols-2) with icon name, title, description.
   - `ClientsPlaceholder.tsx`: fullBleed, id="clients", heading "Trusted By", row of 6 placeholder logo boxes (bg-foreground/5 rounded).
   - `AboutPlaceholder.tsx`: id="about", heading "About", two-column layout (stacked on mobile, side-by-side on desktop) with placeholder image area and bio text.
   - `ProcessPlaceholder.tsx`: id="process", heading "The Process", 4 steps from data in a grid (grid-cols-1 md:grid-cols-2 lg:grid-cols-4).
   - `TestimonialsPlaceholder.tsx`: id="testimonials", heading "Testimonials", grid of testimonial cards from data.
   - `ContactPlaceholder.tsx`: id="contact", heading "Get in Touch", placeholder form layout (Name, Email, Company, Description fields as styled div placeholders -- real form in Phase 3), email link, two buttons.
   - `Footer.tsx`: id="footer", no SectionWrapper (custom padding py-12), max-w-6xl centered, logo text, email, social icon placeholders, copyright.

4. Replace `app/layout.tsx`:
   - Import syne, dmSans from `@/lib/fonts`
   - Remove Geist font imports
   - Apply both font CSS variables on `<html>`: `${syne.variable} ${dmSans.variable}`
   - Body classes: bg-background text-body font-body antialiased
   - Metadata: title "Alex Rivera | Cinematic Videographer", description about videography portfolio

5. Replace `app/page.tsx`:
   - Import all 10 section components
   - Render in order: HeroPlaceholder, WorkPlaceholder, ShowreelPlaceholder, ServicesPlaceholder, ClientsPlaceholder, AboutPlaceholder, ProcessPlaceholder, TestimonialsPlaceholder, ContactPlaceholder, Footer
   - Wrap in `<main>` element

IMPORTANT responsive rules (VIS-04):
- All components must use mobile-first Tailwind classes
- Type scale: h1 text-4xl md:text-5xl lg:text-6xl, h2 text-3xl md:text-4xl, body text-base md:text-lg
- Headings: font-heading font-bold tracking-tight text-heading
- Body text: text-body (warm gray)
- Gold accent only on CTA hover states (per user decision -- 95% monochromatic)
  </action>
  <verify>
    <automated>cd /home/nikolovn/videographer-portfolio && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>All UI and section components created. Layout uses Syne + DM Sans. Page renders all 10 sections in order. Build succeeds with no errors. Site displays dark theme with correct typography, colors, and responsive layout. All placeholder content sourced from data files.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` renders the page with:
   - Dark charcoal background (#0a0a0a)
   - Off-white headings in Syne font
   - Warm gray body text in DM Sans
   - Gold accent visible on CTA button hover
   - All 10 sections visible with generous spacing
   - Responsive layout at mobile (375px), tablet (768px), and desktop (1280px) widths
3. All data imports resolve (no hardcoded content in components)
</verification>

<success_criteria>
- Design tokens (colors, fonts, spacing) established in globals.css with @theme inline
- Syne renders on all headings and nav text, DM Sans on body text
- Champagne gold (#c9a96e) appears only on CTA hover states
- 10 placeholder sections render in correct order with py-32/py-48 spacing
- Data files provide all placeholder content via typed TypeScript exports
- Build passes, no TypeScript errors
- Layout works at 375px, 768px, and 1280px widths
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
