---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on:
  - 01-00
files_modified:
  - lib/fonts.ts
  - lib/types.ts
  - app/globals.css
  - data/navigation.ts
  - data/projects.ts
  - data/services.ts
  - data/site.ts
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "Syne and DM Sans fonts are configured for Next.js with CSS variables"
    - "Design tokens (colors, font mappings) are defined in globals.css via @theme inline"
    - "All shared TypeScript interfaces are exported from lib/types.ts"
    - "All placeholder data files export typed constants sourced from lib/types.ts"
  artifacts:
    - path: "app/globals.css"
      provides: "Tailwind @theme inline tokens for colors, fonts"
      contains: "--color-gold: #c9a96e"
    - path: "lib/fonts.ts"
      provides: "Syne + DM Sans font configuration"
      exports: ["syne", "dmSans"]
    - path: "lib/types.ts"
      provides: "Shared TypeScript interfaces"
      exports: ["NavItem", "Project", "Service", "Testimonial"]
    - path: "data/navigation.ts"
      provides: "Nav links and site config"
      exports: ["navItems", "siteConfig"]
    - path: "data/projects.ts"
      provides: "Placeholder project data"
      exports: ["projects"]
    - path: "data/services.ts"
      provides: "Placeholder service data"
      exports: ["services"]
    - path: "data/site.ts"
      provides: "Site config, process steps, testimonials"
      exports: ["processSteps", "testimonials"]
  key_links:
    - from: "data/*.ts"
      to: "lib/types.ts"
      via: "import type interfaces"
      pattern: "import.*from.*@/lib/types"
    - from: "app/globals.css"
      to: "lib/fonts.ts"
      via: "@theme inline mapping font vars"
      pattern: "--font-heading.*var\\(--font-syne\\)"
---

<objective>
Design tokens, font configuration, shared types, and placeholder data files.

Purpose: Establish the visual foundation (colors, typography tokens) and typed data layer that every component in Plans 02-04 builds on. This is the data and design substrate -- no UI components yet.

Output: globals.css with @theme inline tokens, Syne + DM Sans font config, TypeScript interfaces, and 4 data files with typed placeholder content.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@app/layout.tsx
@app/globals.css
@app/page.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create design tokens, fonts, types, and data files</name>
  <files>
    lib/fonts.ts,
    lib/types.ts,
    app/globals.css,
    data/navigation.ts,
    data/projects.ts,
    data/services.ts,
    data/site.ts,
    package.json
  </files>
  <action>
1. Install dependencies:
   ```bash
   npm install react-intersection-observer lucide-react
   ```

2. Create `lib/fonts.ts` -- Syne + DM Sans via next/font/google:
   - Syne: variable `--font-syne`, subsets ["latin"], display "swap". Do NOT specify weight array (variable font).
   - DM_Sans: variable `--font-dm-sans`, subsets ["latin"], display "swap". Import as `DM_Sans` (underscore). Do NOT specify weight array.
   - Export both font objects.

3. Create `lib/types.ts` -- shared interfaces:
   - `NavItem`: { label: string, href: string, sectionId: string }
   - `Project`: { id: string, title: string, category: string, client: string, year: string, role: string, type: string, thumbnailUrl: string, videoUrl: string, challenge: string, approach: string, result: string, gallery: string[] }
   - `Service`: { id: string, title: string, description: string, icon: string }
   - `Testimonial`: { id: string, quote: string, name: string, company: string }
   - `ProcessStep`: { id: string, number: number, title: string, description: string }
   - `SiteConfig`: { name: string, tagline: string, email: string, instagram: string, linkedin: string, copyright: string, ctaText: string, ctaHref: string }

4. Replace `app/globals.css` entirely with the Phase 1 design tokens:
   ```css
   @import "tailwindcss";

   @theme inline {
     --color-background: #0a0a0a;
     --color-foreground: #f5f5f5;
     --color-heading: #f5f5f5;
     --color-body: #a0a0a0;
     --color-gold: #c9a96e;
     --font-heading: var(--font-syne);
     --font-body: var(--font-dm-sans);
   }

   html {
     scroll-behavior: smooth;
     scroll-padding-top: 5rem;
   }

   body {
     background-color: #0a0a0a;
     color: #a0a0a0;
     font-family: var(--font-dm-sans), ui-sans-serif, system-ui, sans-serif;
   }

   h1, h2, h3, h4, h5, h6 {
     font-family: var(--font-syne), ui-sans-serif, system-ui, sans-serif;
     color: #f5f5f5;
   }
   ```
   Remove the :root, dark media query, and Geist font references from the old file.

5. Create data files with typed placeholder content:
   - `data/navigation.ts`: navItems array (Work, Showreel, Services, About, Contact with matching sectionIds and href="#id") and siteConfig object (name: "Alex Rivera", tagline: "Cinematic Storytelling", ctaText: "Start a Project", ctaHref: "#contact").
   - `data/projects.ts`: 6 placeholder Project objects with realistic videography titles, categories (Commercial, Brand Film, Social Content, Corporate), placeholder URLs.
   - `data/services.ts`: 4 Service objects matching SERV-02 (Commercial Video Production, Brand Story Films, Social Media Content, Event and Corporate Films) with descriptions and lucide icon names.
   - `data/site.ts`: SiteConfig with name, tagline, email "hello@alexrivera.com", instagram/linkedin URLs, copyright text, plus ProcessStep array (Discovery, Concept, Production, Post Production) and Testimonial array (3 placeholder entries).

All data files must import types from `@/lib/types` and export typed constants.
  </action>
  <verify>
    <automated>cd /home/nikolovn/videographer-portfolio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Dependencies installed. lib/fonts.ts exports Syne + DM Sans. lib/types.ts exports all shared interfaces. globals.css has @theme inline tokens with correct colors and font mappings. Four data files export typed placeholder content. TypeScript compilation passes with no errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. All data files import types from @/lib/types
3. globals.css contains @theme inline with --color-gold: #c9a96e
4. lib/fonts.ts exports syne and dmSans font objects
5. `npx vitest run` -- data.test.ts todo tests still listed (no regressions)
</verification>

<success_criteria>
- Design tokens (colors, fonts, spacing) established in globals.css with @theme inline
- Syne and DM Sans fonts configured via next/font/google with CSS variables
- All shared TypeScript interfaces exported from lib/types.ts
- 4 data files with typed placeholder content (navigation, projects, services, site)
- TypeScript compilation passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
